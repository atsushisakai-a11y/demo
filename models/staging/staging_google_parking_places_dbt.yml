version: 2

models:
  - name: staging_google_parking_places_dbt
    description: "DBT-transformed version of Google parking places with demand scoring, demand buckets, and ParkBee readiness."
    columns:
      - name: place_id
        tests:
          - not_null
      - name: name
      - name: demand_score
      - name: demand_category
