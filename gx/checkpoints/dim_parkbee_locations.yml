name: dim_parkbee_locations
config_version: 1

validations:
  - batch_request:
      datasource_name: bigquery_parkbee
      data_connector_name: default_runtime
      data_asset_name: dim_parkbee_locations_dbt
      runtime_parameters:
        query: >
          SELECT *
          FROM `grand-water-473707-r8.dwh.dim_parkbee_locations_dbt`
      batch_identifiers:
        default_identifier_name: latest
    expectation_suite_name: dim_parkbee_locations
