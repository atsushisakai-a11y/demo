config_version: 3
plugins_directory: plugins/

datasources:
  parkbee_bigquery:
    class_name: SqlAlchemyDatasource
    module_name: great_expectations.datasource

    execution_engine:
      class_name: SqlAlchemyExecutionEngine
      # FIXED BigQuery URI (must use pybigquery dialect)
      connection_string: bigquery+pybigquery://grand-water-473707-r8

    data_connectors:
      default_runtime:
        class_name: RuntimeDataConnector
        batch_identifiers:
          - default_identifier_name

stores:
  expectations_store:
    class_name: ExpectationsStore
    store_backend:
      class_name: TupleFilesystemStoreBackend
      base_directory: ${GE_DATA_CONTEXT_ROOT_DIR}/expectations

  validations_store:
    class_name: ValidationsStore
    store_backend:
      class_name: TupleFilesystemStoreBackend
      base_directory: ${GE_DATA_CONTEXT_ROOT_DIR}/uncommitted/validation_results

  checkpoint_store:
    class_name: CheckpointStore
    store_backend:
      class_name: TupleFilesystemStoreBackend
      base_directory: ${GE_DATA_CONTEXT_ROOT_DIR}/checkpoints

expectations_store_name: expectations_store
validations_store_name: validations_store
checkpoint_store_name: checkpoint_store

data_docs_sites:
  local_site:
    class_name: SiteBuilder
    store_backend:
      class_name: TupleFilesystemStoreBackend
      base_directory: ${GE_DATA_CONTEXT_ROOT_DIR}/uncommitted/data_docs/

anonymous_usage_statistics:
  enabled: false
